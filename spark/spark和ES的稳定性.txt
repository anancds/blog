1、spark的worker上分配的执行任务读取数据的量过打，内存不够，导致内存溢出。

2、spark的数据倾斜或者内存不足有可能会导致Executor lost。

3、ES聚合操作太频繁会消耗大量内存，导致OOM。

4、ES的分片在磁盘上分配不均匀会导致查询不稳定。

5、ES有节点下线后会导致一些查询时间不稳定。

6、断电会导致es数据不一致。

7、网络原因都会导致spark和es集群的稳定性。

8、集群脑裂。

9、gc时间太长会导致集群通信出现问题。
